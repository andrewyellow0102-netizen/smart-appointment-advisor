# 智能掛號建議系統 PRD

## 1. 概覽（Vision）

**一句話說清楚：「輸入症狀，AI 告訴你應該看哪一科。」**

台灣醫療資源豐富，但一般人常見問題是「我這個症狀該看哪一科？」本系統用自然語言處理讓使用者描述症狀，AI 分析後推薦合適科別與醫師，大幅減少掛錯科、來回轉診的困擾。

---

## 2. 問題背景（Why）

| 痛點 | 說明 |
|------|------|
| 掛錯科浪費時間 | 重複掛號、等候、浪費医疗资源 |
| 不知道找哪位醫師 | 同一科有許多醫師，無法判斷擅長領域 |
| 就醫路徑不透明 | 不知道自己適合去診所還是醫學中心 |

---

## 3. 目標使用者

- **主要用戶**：有就醫需求但不確定該看哪一科的民眾
- **場景**：身體不適，上網搜尋後仍然困惑，直接使用本系統得到初步建議

---

## 4. MVP 功能範圍

### 4.1 核心功能（MVP）

#### F1：症狀輸入
- 使用者用自然語言描述症狀（如：「頭痛、發燒三天、偶而會想吐」）
- 支援中英文
- 即時輸入，無需提交按鈕（N 秒後自動分析）

#### F2：AI 科別推薦
- 後端接收症狀文字，呼叫 Claude API 分析
- Claude 輸出：推薦科別 + 理由 + 緊急程度（一般/建議就醫/緊急）
- API 端點：`POST /api/analyze`
- Response：
```json
{
  "recommended_department": "神經內科",
  "reason": "頭痛伴隨發燒三日，可能是感染或神經系統問題",
  "urgency": "recommend_visit",
  "alternatives": ["感染科", "家醫科"]
}
```

#### F3：醫師推薦
- 根據推薦科別，從資料集返回 3-5 位醫師
- 每位醫師顯示：姓名、專長、院所、評分、門診時段
- API 端點：`GET /api/doctors?department=<科別>`
- Response：
```json
{
  "doctors": [
    {
      "id": "d001",
      "name": "王大明",
      "department": "神經內科",
      "hospital": "台大醫院",
      "specialty": "頭痛、腦血管疾病",
      "rating": 4.8,
      "appointment_url": "https://..."
    }
  ]
}
```

#### F4：掛號連結
- 每位醫師提供外部掛號連結（導向醫院官網或健保App）
- 不做自己的掛號系統，只做推薦 + 導流

#### F5：症狀歷史記錄
- 使用者的就醫查詢歷史保存在瀏覽器 localStorage
- 可隨時回顧之前查過的症狀與建議

---

### 4.2 不做的功能（Out of Scope）

- 真正的掛號功能（只導連結）
- 醫師預約功能
- 線上問診
- 健康存儲（病歷整合）

---

## 5. 資料模型

### 5.1 醫師資料（靜態 JSON，MVP 階段）
```json
{
  "id": "d001",
  "name": "王大明",
  "departments": ["神經內科"],
  "hospital": "台大醫院",
  "specialties": ["頭痛", "腦血管疾病"],
  "rating": 4.8,
  "review_count": 230,
  "appointment_url": "https://www.ntuh.gov.tw/appointment"
}
```

### 5.2 科別對應
```json
{
  "departments": [
    { "id": "dept001", "name": "家醫科", "keywords": ["一般", "全身", "無特定症狀"] },
    { "id": "dept002", "name": "神經內科", "keywords": ["頭痛", "頭暈", "四肢麻木"] },
    { "id": "dept003", "name": "心臟內科", "keywords": ["胸悶", "心悸", "血壓"] },
    { "id": "dept004", "name": "消化內科", "keywords": ["腹痛", "腹瀉", "嘔吐", "胃食道"] },
    { "id": "dept005", "name": "骨科", "keywords": ["骨頭", "關節", "腰酸背痛"] },
    { "id": "dept006", "name": "泌尿科", "keywords": ["頻尿", "血尿", "腎結石"] },
    { "id": "dept007", "name": "眼科", "keywords": ["眼睛", "視力", "紅眼"] },
    { "id": "dept008", "name": "耳鼻喉科", "keywords": ["喉嚨", "流鼻水", "耳鳴"] },
    { "id": "dept009", "name": "皮膚科", "keywords": ["皮膚", "過敏", "濕疹", "痘痘"] },
    { "id": "dept010", "name": "兒科", "keywords": ["小孩", "發燒", "兒童"] }
  ]
}
```

---

## 6. API 規格

| 端點 | 方法 | 說明 |
|------|------|------|
| `POST /api/analyze` | POST | 接收症狀文字，回傳 AI 分析結果 |
| `GET /api/doctors` | GET | 依科別查詢醫師列表 |
| `GET /api/departments` | GET | 回傳所有科別列表 |
| `GET /health` | GET | 健康檢查 |

### `POST /api/analyze` Request
```json
{ "symptoms": "頭痛發燒三天了，伴隨想吐的感覺" }
```

### `POST /api/analyze` Response
```json
{
  "recommended_department": "神經內科",
  "reason": "持續性頭痛伴隨發燒和嘔吐感，建議優先排除腦膜炎或顱內壓升高的可能性。",
  "urgency": "recommend_visit",
  "urgency_label": "建議盡快就醫",
  "alternatives": ["感染科", "家醫科"]
}
```

---

## 7. 使用者流程

```
開啟網頁
    ↓
輸入癥狀（自然語言）
    ↓
點擊「分析」按鈕（或自動觸發）
    ↓
載入中（顯示 AI 分析中的狀態）
    ↓
呈現推薦結果：
  ├─ 推薦科別 + 緊急程度標示
  ├─ AI 理由說明
  └─ 該科別的醫師列表
    ↓
點擊「掛號」→ 開啟外部掛號頁面
    ↓
歷史記錄自動保存到 localStorage
```

---

## 8. 前端頁面規劃

### Page 1: 首頁（症狀輸入）
- 大標題：「身體不舒服？AI 告訴你該看哪一科」
- 輸入框：多行文字輸入癥狀描述（placeholder：「例：最近一個月常常頭痛，特別是下午...」）
- 「分析」按鈕
- 下方顯示：過去查詢記錄（localStorage）

### Page 2: 結果頁（同一頁切換顯示）
- AI 推薦科別卡片（醒目大字）
- 緊急程度標示（🟢一般 / 🟡建議就醫 / 🔴盡快就醫）
- 推薦理由（AI 生成的說明）
- 備選科別（可點擊切換）
- 醫師列表（卡片式）

---

## 9. 垂直切片規劃

每個 Slice 完成後可独立運作並 demo：

| Slice | 功能 | 完成標準 |
|-------|------|---------|
| **Slice 1** | 基本系統架構 + 症狀輸入 | 輸入癥狀點擊送出，畫面正確請求後端 |
| **Slice 2** | AI 科別推薦（Claude API 串接） | 輸入癥狀 → 正確显示 AI 分析結果 |
| **Slice 3** | 醫師推薦列表呈現 | 顯示醫師卡片，點擊可開啟掛號連結 |
| **Slice 4** | 緊急程度標示 + 備選科別 | 緊急/警告/一般三級標示 + 備選科切換 |
| **Slice 5** | 癥狀歷史（localStorage） | 查詢歷史可查閱 |
| **Slice 6** | UI/UX 優化 | 響應式設計、載入狀態、過場動畫 |

---

## 10. 技術架構

```
┌──────────────────────────────────┐
│         使用者瀏覽器              │
│   (HTML + Vanilla JS + Tailwind)  │
└──────────────┬───────────────────┘
               │ HTTP
               ↓
┌──────────────────────────────────┐
│      FastAPI Backend (Python)    │
│   ├─ POST /api/analyze           │
│   │    └→ 叫用 Claude API        │
│   ├─ GET /api/doctors            │
│   └─ GET /api/departments        │
└──────────────┬───────────────────┘
               │
          ┌────┴────┐
          ↓         ↓
   Claude API   JSON 檔案
   (AI 分析)    (醫師資料)
```

---

## 11. 環境變數

```
ANTHROPIC_API_KEY=sk-ant-xxxxx  # Claude API Key
```

---

## 12. 驗收標準（Acceptance Criteria）

- [ ] 使用者輸入「頭痛發燒三天」，系統回傳「神經內科」或「感染科」
- [ ] 緊急程度正確標示
- [ ] 每個推薦科別顯示至少 3 位醫師
- [ ] 醫師卡片包含：姓名、院所、專長、評分
- [ ] 點擊醫師卡片可開啟掛號連結（新分頁）
- [ ] 過去查詢記錄保存在 localStorage
- [ ] 手機版介面可用（響應式）
- [ ] API 回應時間 < 5 秒（不含 Claude API 延遲）
- [ ] 前端無明顯 JS 錯誤

---

## 13. 緊急程度定義

| 等級 | 標示 | 時機 |
|------|------|------|
| `routine` | 🟢 一般 | 慢性症狀、保養諮詢 |
| `recommend_visit` | 🟡 建議就醫 | 持續數天的輕微不適 |
| `urgent` | 🔴 盡快就醫 | 嚴重或急性症狀 |
| `emergency` | 🚨 急診 | 可能危及生命（直接提示叫救護車）|

---

## 14. 備註

- MVP 階段醫師資料為靜態 JSON，暫不串接真實醫療資料庫
- AI 推薦僅供參考，不作為醫療建議，須加註免责聲明
- Claude API 使用 function calling 確保輸出格式穩定

---

*PRD 版本：1.0*
*建立時間：2025-05-26*